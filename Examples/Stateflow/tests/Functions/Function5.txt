%type: module

module Chart():
begin
  ChartEL := [];
  b := [[0,1],[2,3]];
  Chart_st := "Chart_A";
  s := "en_A";
  log("en_A" + "\n");
  {
    Chart_ret := 0;
    if (Chart_st == "Chart_A") {
      Chart_A_done := 0;
      if (b[0][0] < b[1][1]) {
        b[0][0] := b[1][1];
      } else {
        skip;
      }
      if (b[1][0] < b[0][1]) {
        b[1][0] := b[0][1];
      } else {
        skip;
      }
      if (Chart_st == "Chart_A") {
        J29_done := 0;
        if (b[0][0] == 3 && b[0][1] == 1) {
          if (Chart_st == "Chart_A") {
            Chart_st := "";
          }
          if (Chart_st == "") {
            Chart_st := "Chart_B";
            s := "en_B";
            log("en_B" + "\n");
          }
          J29_done := 1;
        }
        Chart_ret := J29_done;
        Chart_A_done := Chart_ret;
      }
      Chart_ret := Chart_A_done;
    } else if (Chart_st == "Chart_B") {
      Chart_ret := 0;
    }
    wait(0.1);
  }*
end
endmodule
system
Chart()
endsystem
