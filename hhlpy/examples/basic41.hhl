# ArchiveEntry "Benchmarks/Basic/STTT Tutorial: Example 2"
pre [v >= 0] [A > 0] [B > 0];
{
  { a := A; }
  ++
  { a := 0; }
  ++
  { a := -B; }
  t := * (t >= 0);
  {x_dot = v, v_dot = a, t_dot = -1 & t > 0 && v > 0}
    invariant [true];
}*
  invariant [v >= 0];
post [v >= 0];