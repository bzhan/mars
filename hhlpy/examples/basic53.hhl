# Basic benchmark, problem 53
#         v>=0  & A>=0 & b>0
#  -> [
#       {
#         {a:=A; ++ a:=-b;}
#         {x'=v, v'=a & v>=0}
#       }*@invariant(v>=0)
#     ] v>=0
pre [v >= 0] [A >= 0] [b > 0];
{ 
  a := A; ++ a := -b; 
  <x_dot = v, v_dot = a & v > 0> invariant [true];
}* invariant [v >= 0];
post [v >= 0];