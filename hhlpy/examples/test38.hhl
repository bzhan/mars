# Basic benchmark, problem 31
# {x + z >= 0} 
# <x_dot = x^2, z_dot = z * x + y & y > x^2>
# {x + z >= 0}
# tag, info, str1 =
pre [x + z >= 0];
<x_dot = x^2, z_dot = z * x + y & y > x^2> invariant [x + z >= 0] {dbx x};
post [x + z >= 0];