pre;
ChartEL := [];
x := 0;
Chart_st := "Chart_A";
{
  Chart_A_done := 0;
  if (x >= 1) {
    x := 0;
    Chart_st := "Chart_A";
    Chart_A_done := 1;
  }
  Chart_ret := Chart_A_done;
  {x_dot = 1 & x < 1}
}*
  invariant [0 <= x] [x <= 1];
post [0 <= x] [x <= 1];