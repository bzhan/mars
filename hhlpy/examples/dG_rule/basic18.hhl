# ArchiveEntry "Benchmarks/Basic/Dynamics: Exponential growth (1)"

pre [x >= 0];
t := * (t >= 0);
{x_dot = x, t_dot = -1 & t > 0} 
invariant 
  ghost (y_dot = - y)
  ghost z 
  [y * z * z == 1] 
  [y > 0] 
  [x * y >= 0];
post [x >= 0];