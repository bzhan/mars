P ::= @Chart || @Chart1
Chart ::= a_Chart := 0; a_Chart_ENTRY := 0; a_Chart_A := 0; a_Chart_B := 0; a_Chart := 1; a_Chart_ENTRY := 1; wait(2); (if a_Chart_ENTRY == 1 then done := 0; done == 0 -> (a_Chart_ENTRY := 0; a_Chart_A := 1; out_tri := 0; ch_trig!out_tri; wait(0.2); state_time := state_time+2; out_tri := 1; ch_trig!out_tri; out_tri := 1; ch_trig!out_tri; wait(0.2); state_time := state_time+2; out_tri := 0; ch_trig!out_tri; out_tri := 0; ch_trig!out_tri; wait(0.2); state_time := state_time+2; out_tri := 1; ch_trig!out_tri; out_tri := 1; ch_trig!out_tri; wait(0.2); state_time := state_time+2; out_tri := 0; ch_trig!out_tri; done := 1) elif a_Chart_A == 1 then done := 0; done == 0 -> (a_Chart_A := 0; a_Chart_B := 1; done := 1) elif a_Chart_B == 1 then skip else skip endif; wait(2))**
Chart1 ::= y := 0; a_Chart1 := 0; wait(2); ch_trig?osig; (ch_trig?out_tri; osig != out_tri -> (a_S1 == 0 -> (a_Chart1 := 1; y := y+1; osig := out_tri; wait(2); state_time := state_time+2; ch_trig?out_tri); skip); osig := out_tri; wait(2); state_time := state_time+2; "true" == "true" -> a_S1 := 0)**
