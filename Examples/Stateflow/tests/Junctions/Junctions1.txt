%type: module

module Chart():
begin
  ChartEL := [];
  y := 4;
  Chart_st := "Chart_A";
  s := "enA";
  log("enA" + "\n");
  (
    Chart_ret := 0;
    if Chart_st == "Chart_B" then
      Chart_ret := 0
    elif Chart_st == "Chart_A" then
      Chart_A_done := 0;
      Chart_st == "Chart_A" -> (
        Chart_st := "Chart_D";
        s := "enD";
        log("enD" + "\n");
        Chart_A_done := 1
      );
      Chart_st == "Chart_A" && Chart_A_done == 0 -> (
        s := "duA";
        log("duA" + "\n")
      );
      Chart_ret := Chart_A_done
    elif Chart_st == "Chart_C" then
      Chart_ret := 0
    elif Chart_st == "Chart_E" then
      Chart_ret := 0
    elif Chart_st == "Chart_D" then
      Chart_ret := 0
    else
      skip
    endif;
    wait(0.1)
  )**
end
endmodule
system
Chart = Chart()
endsystem
