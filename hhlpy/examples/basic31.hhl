# ArchiveEntry "Benchmarks/Basic/Dynamics: Darboux inequality"

pre [x + z >= 0];
t := * (t >= 0);
{x_dot = x^2, z_dot = z * x + y, t_dot = -1 & t > 0 && y > x^2}
  invariant [x + z >= 0] {dbx x};
post [x + z >= 0];