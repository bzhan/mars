D ::= @M || @S1
M ::= num := 0; (num == 0 -> (E := "e"; EL := []; EL := push(EL, E); NL := []; NL := push(NL, 1); num := 1); num == 1 -> (BC1!E --> skip $ BR1?E --> EL := push(EL, E); NL := push(NL, 1); num := 1 $ BO1? --> num := num+1; NL := pop(NL); NL := push(NL, 1)); num == 2 -> (EL := pop(EL); NL := pop(NL); EL == [] -> num := 0; EL != [] -> (E := top(EL); num := top(NL))))**
S1 ::= a_S1 := 0; a_pushing := 0; a_idle := 0; a_S1 := 1; unsent := 5; cansend := 1; a_idle := 1; (BC1?E; if a_pushing == 1 then done := 0; unsent <= 0 && done == 0 -> (a_pushing := 0; a_idle := 1; done := 1); cansend == 1 && done == 0 -> (a_pushing := 0; unsent := unsent-1; a_pushing := 1; done := 1); done == 0 -> flag := 0 elif a_idle == 1 then done := 0; unsent > 0 && done == 0 -> (a_idle := 0; a_pushing := 1; done := 1); done == 0 -> flag := 1 else skip endif; BO1!)**